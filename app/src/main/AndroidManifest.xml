<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.final2">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signup" />
        <activity android:name=".accountType" />
        <activity android:name=".studentUI" />
        <activity android:name=".searchResult" />
        <activity android:name=".createUser" />
        <activity android:name=".uploadingImage" />
        <activity android:name=".testing" />
        <activity android:name=".testing2" />
        <activity android:name=".createuserstudent" />
        <activity android:name=".TutorUI"></activity>
    </application>

</manifest>